# Only the required fields; everything else takes its default.
schema: 1
name: "Synthetic Minimal Project"
slug: minimal-defaults
mode: solo
visibility: closed
people:
  - name: "Researcher A"
    role: lead
