#!/bin/sh
# Data-exposure checks on the staged files: declared dataset paths, forbidden file types, file
# size, named secret patterns, and a warning-only scan for personal identifiers.
exec uv run --locked build.py check --staged
